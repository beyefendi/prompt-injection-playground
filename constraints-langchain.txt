# Unified constraints for langchain family across spikee and llm-webmail
# Goal: keep all langchain packages on the 0.3.x line and avoid accidental 1.x upgrades
# Pip uses hyphen/underscore normalization, so constraints apply regardless of style.

# Core LangChain packages
langchain-core<1.0.0
langchain<1.0.0
langchain-community<1.0.0

# Provider integrations that depend on langchain-core
langchain-openai<1.0.0
langchain-together<1.0.0 # Problematic package that often lags behind core
langchain-aws<1.0.0
langchain-ollama<1.0.0
langchain-anthropic<1.0.0
langchain-groq<1.0.0
langchain-huggingface<1.0.0

# Separate release train (works with core 0.3.x)
langchain-google-genai<3.0.0

# Telemetry/client library used by langchain
langsmith<0.2.0
